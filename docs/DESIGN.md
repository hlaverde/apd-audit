# DESIGN.md — pending

**Status:** placeholder. This document will be written **after** the POC has
run successfully end-to-end. The user explicitly asked to pause at that point
so they can review POC outputs and decide whether to scale to the full study.

When written, this document will contain:

1. Restated research questions and H1–H5 in author's own words after re-reading
   the binding spec.
2. The APD indicator — definition rederived, choices justified (why W₁, why
   status weights, why signed Δ).
3. Identification strategy per hypothesis (treatment, control, threats,
   robustness).
4. Data plan — every source, URL, retrieval procedure, license, with the
   zero-cost confirmation.
5. Translation strategy for prompts in 4 languages + 2 indigenous variants
   without paid services.
6. Computational budget showing the arithmetic of fitting 12 000 main + 3 200
   robustness images into the Colab/Kaggle/HF free quotas over 4–6 weeks.
7. Pre-registration checklist for OSF: what is confirmatory, what is
   exploratory.
8. Open questions for the user.

Until then, the binding spec lives in `Propuesta_SciReports_v1.docx`, the
implementation reality lives in the code, and the decisions taken so far live
in `DECISIONS.md`.
